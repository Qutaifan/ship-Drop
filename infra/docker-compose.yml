# Hermes-Ecom storefront stack — self-hosted, free/OSS only (AGENTS.md §7).
#
# PREREQUISITE: the Medusa app must exist at ../medusa before this will build.
#   npx create-medusa-app@latest medusa
# Medusa publishes no official image by design — you build your own project.
#
# Bring up:  docker compose --env-file .env up -d --build
# Exposed only to localhost; Cloudflare Tunnel provides public ingress.

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10

  valkey:
    image: valkey/valkey:8-alpine
    restart: unless-stopped
    command: ["valkey-server", "--save", "60", "1", "--appendonly", "no"]
    volumes:
      - valkeydata:/data
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  # PROTOCOL note: Medusa v2 requires TWO processes. The server handles HTTP;
  # the worker handles scheduled jobs, subscribers and event processing.
  # Running server-only silently drops background work.
  medusa-server:
    build: ../medusa
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      valkey: {condition: service_healthy}
    environment:
      MEDUSA_WORKER_MODE: server
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      COOKIE_SECRET: ${COOKIE_SECRET}
      STORE_CORS: ${STORE_CORS}
      ADMIN_CORS: ${ADMIN_CORS}
      AUTH_CORS: ${AUTH_CORS}
    ports:
      - "127.0.0.1:9000:9000"

  medusa-worker:
    build: ../medusa
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      valkey: {condition: service_healthy}
    environment:
      MEDUSA_WORKER_MODE: worker
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      COOKIE_SECRET: ${COOKIE_SECRET}

  # Storefront (Next.js) — built from Medusa starter
  storefront:
    build: ../medusa-storefront
    restart: unless-stopped
    depends_on:
      - medusa-server
    environment:
      NEXT_PUBLIC_MEDUSA_BACKEND_URL: ${NEXT_PUBLIC_MEDUSA_BACKEND_URL}
      NEXT_PUBLIC_BASE_URL: ${NEXT_PUBLIC_BASE_URL}
      MEDUSA_BACKEND_URL: ${MEDUSA_BACKEND_URL}
    ports:
      - "127.0.0.1:8000:8000"

  umami:
    image: ghcr.io/umami-software/umami:postgresql-latest
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
    environment:
      DATABASE_URL: ${DATABASE_URL}
      APP_SECRET: ${JWT_SECRET}
      HASH_SALT: ${UMAMI_HASH_SALT}
    ports:
      - "127.0.0.1:3000:3000"

  uptime-kuma:
    image: louislam/uptime-kuma:1-alpine
    restart: unless-stopped
    volumes:
      - uptimekumadata:/app/data
    ports:
      - "127.0.0.1:3001:3001"

  # Listmonk — self-hosted email sequences (AGPL-3.0)
  listmonk:
    image: listmonk/listmonk:latest
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
    environment:
      LISTMONK_DATABASE_HOST: postgres
      LISTMONK_DATABASE_USER: ${POSTGRES_USER}
      LISTMONK_DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      LISTMONK_DATABASE_NAME: ${POSTGRES_DB}
      LISTMONK_DATABASE_SSL_MODE: disable
      LISTMONK_APP_ADMIN_USERNAME: ${LISTMONK_ADMIN_USER}
      LISTMONK_APP_ADMIN_PASSWORD: ${LISTMONK_ADMIN_PASSWORD}
      LISTMONK_APP_ADDRESS: 0.0.0.0:9000
    ports:
      - "127.0.0.1:9001:9000"

volumes:
  pgdata:
  valkeydata:
  uptimekumadata: